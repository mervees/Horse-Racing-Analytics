# Core (required)
pandas>=2.1
numpy>=1.26
scikit-learn>=1.3
scipy>=1.11
pyyaml>=6.0

# Gradient-boosted ranker (recommended). If absent, the code falls back
# to sklearn's HistGradientBoostingClassifier automatically.
lightgbm>=4.1

# Plotting (optional; only used by notebooks / ad-hoc analysis)
matplotlib>=3.7

# LLM + RAG features (optional). Without these, explanation/assistant code
# degrades to deterministic templates and TF-IDF retrieval (sklearn).
# anthropic>=0.40
# voyageai>=0.2

# Data download (optional; only for scripts/download_kaggle.py)
# kaggle>=1.6
